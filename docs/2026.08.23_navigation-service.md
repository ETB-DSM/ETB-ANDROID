# Phase 7 — NavigationService + 길안내 엔진 연동

## 개요

Foreground Service 기반의 길안내 엔진을 구현한다.
- **LocationCollector**: FusedLocationProviderClient로 2초 간격 GPS 수신
- **ActionCalculator**: Haversine 거리 계산으로 현재 action 결정
- **ActionUploader**: action 변경 시 즉시, 변경 없으면 30초 keep-alive 전송
- **T-Map 보행자 경로**: `POST /tmap/routes/pedestrian` → RouteStep 목록
- **NavigationRepository 확장**: fetchRoute / createInstruction / updateStatus
- **NavigationScreen 연결**: ViewModel을 통해 실시간 action 표시

---

## 생성·수정 파일 목록

```
app/src/main/java/com/aicane/app/
  domain/
    model/
      TurnType.kt                           # (신규) enum: STRAIGHT, LEFT, RIGHT
      RouteStep.kt                          # (신규) 경로 단계 도메인 모델
      NavigationAction.kt                   # (신규) enum: STRAIGHT, PREPARE_LEFT, LEFT, PREPARE_RIGHT, RIGHT, ARRIVED, REROUTE
      NavigationInstruction.kt              # (신규) action 전송 도메인 모델
    repository/
      NavigationRepository.kt               # (수정) fetchRoute / createInstruction / updateStatus 추가
    usecase/navigation/
      FetchRouteUseCase.kt                  # (신규)
      CreateInstructionUseCase.kt           # (신규)
      UpdateSessionStatusUseCase.kt         # (신규)
  data/
    remote/
      api/
        TmapPedestrianApi.kt                # (신규) @TmapClient Retrofit POST /tmap/routes/pedestrian
      dto/
        tmap/
          TmapPedestrianResponse.kt         # (신규) GeoJSON FeatureCollection DTO
        navigation/
          CreateInstructionRequest.kt       # (신규)
          UpdateStatusRequest.kt            # (신규)
    repository/
      NavigationRepositoryImpl.kt           # (수정) fetchRoute / createInstruction / updateStatus 구현 추가
  di/
    NavigationModule.kt                     # (수정) TmapPedestrianApi 추가 provide

  navigation/
    NavigationConfig.kt                     # (신규) Hilt @Singleton — sessionId, steps, dest 공유 상태
    NavigationStateHolder.kt                # (신규) Hilt @Singleton — 현재 instruction StateFlow
    LocationCollector.kt                    # (신규) FusedLocationProviderClient 래퍼
    ActionCalculator.kt                     # (신규) Haversine + step 매칭 + action 결정
    ActionUploader.kt                       # (신규) 즉시/keep-alive 전송 로직

  service/
    NavigationService.kt                    # (신규) @AndroidEntryPoint Foreground Service

  presentation/
    navigation/
      NavigationViewModel.kt                # (신규) 경로 조회 + 서비스 시작 + UiState
    destination/
      DestinationListViewModel.kt           # (수정) NavigateToNavigation 이벤트에 Destination 추가

nav/
  Screen.kt                                 # (수정) Navigation 라우트에 dest 파라미터 추가
  NavGraph.kt                               # (수정) DestinationList 이벤트 + Navigation composable 수정

ui/screen/navigation/
  NavigationScreen.kt                       # (수정) ViewModel 연결, 권한 요청, 실시간 action 표시

AndroidManifest.xml                         # (수정) NavigationService 선언 추가
gradle/libs.versions.toml                   # (수정) play-services-location 추가
app/build.gradle.kts                        # (수정) play-services-location 추가
```

---

## 의존성 추가

### libs.versions.toml
```toml
[versions]
playServicesLocation = "21.3.0"

[libraries]
play-services-location = { group = "com.google.android.gms", name = "play-services-location", version.ref = "playServicesLocation" }
```

### app/build.gradle.kts
```kotlin
implementation(libs.play.services.location)
```

---

## Domain Layer

### 모델

```kotlin
// TurnType.kt
enum class TurnType { STRAIGHT, LEFT, RIGHT }

// RouteStep.kt
data class RouteStep(
    val latitude: Double,
    val longitude: Double,
    val distanceMeters: Double,  // 이 waypoint에서 다음 waypoint까지 거리
    val turnType: TurnType,
    val description: String,
)
// 비고: TRD의 heading은 ActionCalculator에서 불필요 → lat/lng 직접 사용

// NavigationAction.kt
enum class NavigationAction { STRAIGHT, PREPARE_LEFT, LEFT, PREPARE_RIGHT, RIGHT, ARRIVED, REROUTE }

// NavigationInstruction.kt
data class NavigationInstruction(
    val sessionId: String,
    val action: NavigationAction,
    val distanceMeters: Double,
    val message: String,
)
```

### NavigationRepository 확장

```kotlin
interface NavigationRepository {
    suspend fun createSession(destinationId: String): Result<NavigationSession>          // 기존
    suspend fun fetchRoute(startLat: Double, startLng: Double, endLat: Double, endLng: Double): Result<List<RouteStep>>  // 신규
    suspend fun createInstruction(sessionId: String, action: NavigationAction, distanceMeters: Double, message: String): Result<Unit>  // 신규
    suspend fun updateStatus(sessionId: String, status: String): Result<Unit>            // 신규
}
```

### UseCases

```kotlin
class FetchRouteUseCase @Inject constructor(private val repo: NavigationRepository) {
    suspend operator fun invoke(startLat: Double, startLng: Double, endLat: Double, endLng: Double): Result<List<RouteStep>> =
        repo.fetchRoute(startLat, startLng, endLat, endLng)
}

class CreateInstructionUseCase @Inject constructor(private val repo: NavigationRepository) {
    suspend operator fun invoke(sessionId: String, action: NavigationAction, distanceMeters: Double, message: String): Result<Unit> =
        repo.createInstruction(sessionId, action, distanceMeters, message)
}

class UpdateSessionStatusUseCase @Inject constructor(private val repo: NavigationRepository) {
    suspend operator fun invoke(sessionId: String, status: String): Result<Unit> =
        repo.updateStatus(sessionId, status)
}
```

---

## Data Layer

### T-Map 보행자 경로 DTO

```kotlin
// TmapPedestrianResponse.kt — GeoJSON FeatureCollection
@Serializable data class TmapPedestrianResponse(val features: List<TmapFeature>)
@Serializable data class TmapFeature(val geometry: TmapGeometry, val properties: TmapRouteProperties)
@Serializable data class TmapGeometry(val type: String, val coordinates: JsonElement)
@Serializable data class TmapRouteProperties(
    val turnType: Int = 11,
    val distance: Double = 0.0,
    val description: String = "",
)
```

TurnType 매핑:
- `[12, 16, 18, 212]` → LEFT
- `[13, 17, 19, 213]` → RIGHT
- 그 외 → STRAIGHT

### Navigation DTOs

```kotlin
@Serializable data class CreateInstructionRequest(
    val action: String,        // NavigationAction.name.lowercase()
    val distanceMeters: Double,
    val message: String,
)
@Serializable data class UpdateStatusRequest(val status: String)
```

### TmapPedestrianApi

```kotlin
interface TmapPedestrianApi {
    @POST("tmap/routes/pedestrian")
    suspend fun getPedestrianRoute(
        @Query("version") version: Int = 1,
        @Body request: TmapPedestrianRequest,
    ): TmapPedestrianResponse
}

@Serializable data class TmapPedestrianRequest(
    val startX: String,   // longitude
    val startY: String,   // latitude
    val endX: String,
    val endY: String,
    val reqCoordType: String = "WGS84GEO",
    val resCoordType: String = "WGS84GEO",
    val startName: String = "출발지",
    val endName: String = "목적지",
)
```

### NavigationRepositoryImpl 추가 구현

```kotlin
// fetchRoute
override suspend fun fetchRoute(startLat, startLng, endLat, endLng): Result<List<RouteStep>> = runCatching {
    val response = tmapPedestrianApi.getPedestrianRoute(
        request = TmapPedestrianRequest(
            startX = startLng.toString(), startY = startLat.toString(),
            endX = endLng.toString(),   endY = endLat.toString(),
        )
    )
    response.features
        .filter { it.geometry.type == "Point" }
        .map { feature ->
            val coords = feature.geometry.coordinates.jsonArray
            val longitude = coords[0].jsonPrimitive.double
            val latitude  = coords[1].jsonPrimitive.double
            val turn = when (feature.properties.turnType) {
                12, 16, 18, 212 -> TurnType.LEFT
                13, 17, 19, 213 -> TurnType.RIGHT
                else            -> TurnType.STRAIGHT
            }
            RouteStep(latitude, longitude, feature.properties.distance, turn, feature.properties.description)
        }
}

// createInstruction
override suspend fun createInstruction(sessionId, action, distanceMeters, message): Result<Unit> = runCatching {
    navigationApi.createInstruction(
        sessionId = sessionId,
        request = CreateInstructionRequest(action.name.lowercase(), distanceMeters, message),
    )
}

// updateStatus
override suspend fun updateStatus(sessionId, status): Result<Unit> = runCatching {
    navigationApi.updateStatus(sessionId, UpdateStatusRequest(status))
}
```

NavigationApi에 추가:
```kotlin
@POST("api/v1/navigation/sessions/{sessionId}/instructions")
suspend fun createInstruction(@Path("sessionId") sessionId: String, @Body request: CreateInstructionRequest)

@PATCH("api/v1/navigation/sessions/{sessionId}/status")
suspend fun updateStatus(@Path("sessionId") sessionId: String, @Body request: UpdateStatusRequest)
```

### NavigationModule 수정

```kotlin
companion object {
    // 기존: provideNavigationApi (@AppClient)
    @Provides @Singleton
    fun provideTmapPedestrianApi(@TmapClient retrofit: Retrofit): TmapPedestrianApi =
        retrofit.create(TmapPedestrianApi::class.java)
}
```

NavigationRepositoryImpl 생성자에 `TmapPedestrianApi` 추가 주입.

---

## 길안내 엔진

### NavigationConfig (Hilt @Singleton)

```kotlin
@Singleton
class NavigationConfig @Inject constructor() {
    @Volatile var sessionId: String = ""
    @Volatile var steps: List<RouteStep> = emptyList()
    @Volatile var destinationLat: Double = 0.0
    @Volatile var destinationLng: Double = 0.0
    @Volatile var destinationRadius: Double = 30.0
}
```

### NavigationStateHolder (Hilt @Singleton)

```kotlin
@Singleton
class NavigationStateHolder @Inject constructor() {
    val currentInstruction: MutableStateFlow<NavigationInstruction?> = MutableStateFlow(null)
    fun reset() { currentInstruction.value = null }
}
```

### LocationCollector

```kotlin
@Singleton
class LocationCollector @Inject constructor(@ApplicationContext context: Context) {
    private val fusedClient = LocationServices.getFusedLocationProviderClient(context)

    @SuppressLint("MissingPermission")
    fun startUpdates(onLocation: (Location) -> Unit): LocationCallback {
        val request = LocationRequest.Builder(Priority.PRIORITY_HIGH_ACCURACY, 2000L)
            .setMinUpdateIntervalMillis(1000L)
            .build()
        val callback = object : LocationCallback() {
            override fun onLocationResult(result: LocationResult) {
                result.lastLocation?.let(onLocation)
            }
        }
        fusedClient.requestLocationUpdates(request, callback, Looper.getMainLooper())
        return callback
    }

    fun stopUpdates(callback: LocationCallback) = fusedClient.removeLocationUpdates(callback)
}
```

### ActionCalculator

```kotlin
class ActionCalculator @Inject constructor() {

    fun calculate(
        lat: Double, lng: Double,
        steps: List<RouteStep>,
        destLat: Double, destLng: Double, destRadius: Double,
    ): Pair<NavigationAction, Double> {
        // 1. 목적지 도착 판단
        val distToDest = haversine(lat, lng, destLat, destLng)
        if (distToDest <= destRadius) return NavigationAction.ARRIVED to 0.0

        // 2. 경로 이탈 판단: 모든 step 중 최단 거리 > 30m
        if (steps.isEmpty()) return NavigationAction.STRAIGHT to distToDest
        val minDistToRoute = steps.minOf { haversine(lat, lng, it.latitude, it.longitude) }
        if (minDistToRoute > 30.0) return NavigationAction.REROUTE to 0.0

        // 3. 현재 step (가장 가까운 waypoint)
        val currentIdx = steps.indices.minByOrNull { haversine(lat, lng, steps[it].latitude, steps[it].longitude) }
            ?: return NavigationAction.STRAIGHT to distToDest

        // 4. 다음 회전 step 탐색
        val nextTurn = steps.drop(currentIdx + 1).firstOrNull { it.turnType != TurnType.STRAIGHT }
            ?: return NavigationAction.STRAIGHT to distToDest

        val distToTurn = haversine(lat, lng, nextTurn.latitude, nextTurn.longitude)

        // 5. 거리 기반 action 결정
        return when {
            distToTurn <= 5.0  -> when (nextTurn.turnType) {
                TurnType.LEFT  -> NavigationAction.LEFT
                TurnType.RIGHT -> NavigationAction.RIGHT
                else           -> NavigationAction.STRAIGHT
            } to distToTurn
            distToTurn <= 20.0 -> when (nextTurn.turnType) {
                TurnType.LEFT  -> NavigationAction.PREPARE_LEFT
                TurnType.RIGHT -> NavigationAction.PREPARE_RIGHT
                else           -> NavigationAction.STRAIGHT
            } to distToTurn
            else -> NavigationAction.STRAIGHT to distToTurn
        }
    }

    // Haversine 공식 (단위: 미터)
    fun haversine(lat1: Double, lng1: Double, lat2: Double, lng2: Double): Double {
        val R = 6371000.0
        val φ1 = Math.toRadians(lat1); val φ2 = Math.toRadians(lat2)
        val Δφ = Math.toRadians(lat2 - lat1); val Δλ = Math.toRadians(lng2 - lng1)
        val a = sin(Δφ / 2).pow(2) + cos(φ1) * cos(φ2) * sin(Δλ / 2).pow(2)
        return R * 2 * atan2(sqrt(a), sqrt(1 - a))
    }
}
```

### ActionUploader

```kotlin
@Singleton
class ActionUploader @Inject constructor(private val createInstructionUseCase: CreateInstructionUseCase) {
    private var lastAction: NavigationAction? = null
    private var lastUploadTime: Long = 0L

    suspend fun upload(instruction: NavigationInstruction) {
        val now = System.currentTimeMillis()
        if (instruction.action != lastAction || now - lastUploadTime >= 30_000L) {
            createInstructionUseCase(
                instruction.sessionId, instruction.action,
                instruction.distanceMeters, instruction.message,
            ).onSuccess {
                lastAction = instruction.action
                lastUploadTime = now
            }
            // 실패 시: 로그 기록 후 다음 주기 재시도 (세션 유지)
        }
    }

    fun reset() { lastAction = null; lastUploadTime = 0L }
}
```

### NavigationService

```kotlin
@AndroidEntryPoint
class NavigationService : Service() {

    @Inject lateinit var locationCollector: LocationCollector
    @Inject lateinit var actionCalculator: ActionCalculator
    @Inject lateinit var actionUploader: ActionUploader
    @Inject lateinit var fetchRouteUseCase: FetchRouteUseCase
    @Inject lateinit var updateStatusUseCase: UpdateSessionStatusUseCase
    @Inject lateinit var stateHolder: NavigationStateHolder
    @Inject lateinit var config: NavigationConfig

    private val serviceScope = CoroutineScope(SupervisorJob() + Dispatchers.IO)
    private var locationCallback: LocationCallback? = null
    @Volatile private var isRerouting = false

    companion object {
        const val NOTIFICATION_ID = 1001
        const val CHANNEL_ID = "navigation_channel"
    }

    override fun onCreate() {
        super.onCreate()
        createNotificationChannel()
    }

    override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
        startForeground(NOTIFICATION_ID, buildNotification())
        startLocationUpdates()
        return START_STICKY
    }

    private fun startLocationUpdates() {
        locationCallback = locationCollector.startUpdates { location ->
            serviceScope.launch {
                val (action, distance) = actionCalculator.calculate(
                    location.latitude, location.longitude,
                    config.steps,
                    config.destinationLat, config.destinationLng, config.destinationRadius,
                )
                val instruction = NavigationInstruction(
                    sessionId = config.sessionId,
                    action = action,
                    distanceMeters = distance,
                    message = action.toKoreanMessage(distance),
                )
                stateHolder.currentInstruction.value = instruction
                actionUploader.upload(instruction)

                when (action) {
                    NavigationAction.ARRIVED -> {
                        updateStatusUseCase(config.sessionId, "arrived")
                        stopSelf()
                    }
                    NavigationAction.REROUTE -> handleReroute(location.latitude, location.longitude)
                    else -> {}
                }
            }
        }
    }

    private suspend fun handleReroute(lat: Double, lng: Double) {
        if (isRerouting) return
        isRerouting = true
        fetchRouteUseCase(lat, lng, config.destinationLat, config.destinationLng)
            .onSuccess { newSteps ->
                config.steps = newSteps
                actionUploader.reset()
            }
        isRerouting = false
    }

    override fun onDestroy() {
        locationCallback?.let { locationCollector.stopUpdates(it) }
        serviceScope.cancel()
        super.onDestroy()
    }

    override fun onBind(intent: Intent?) = null

    private fun createNotificationChannel() {
        val channel = NotificationChannel(CHANNEL_ID, "길안내", NotificationManager.IMPORTANCE_LOW)
        getSystemService(NotificationManager::class.java).createNotificationChannel(channel)
    }

    private fun buildNotification() = NotificationCompat.Builder(this, CHANNEL_ID)
        .setContentTitle("길안내 진행 중")
        .setSmallIcon(android.R.drawable.ic_menu_directions)
        .setOngoing(true)
        .build()
}

// 확장 함수 (NavigationService.kt 하단)
private fun NavigationAction.toKoreanMessage(distance: Double): String = when (this) {
    NavigationAction.STRAIGHT      -> "직진"
    NavigationAction.PREPARE_LEFT  -> "${distance.toInt()}m 앞 좌회전"
    NavigationAction.LEFT          -> "좌회전"
    NavigationAction.PREPARE_RIGHT -> "${distance.toInt()}m 앞 우회전"
    NavigationAction.RIGHT         -> "우회전"
    NavigationAction.ARRIVED       -> "목적지 도착"
    NavigationAction.REROUTE       -> "경로 재탐색 중"
}
```

---

## Presentation Layer

### NavigationViewModel

```kotlin
sealed class NavigationEvent { object NavigateToEnd : NavigationEvent() }

@HiltViewModel
class NavigationViewModel @Inject constructor(
    private val fetchRouteUseCase: FetchRouteUseCase,
    private val updateStatusUseCase: UpdateSessionStatusUseCase,
    private val stateHolder: NavigationStateHolder,
    private val config: NavigationConfig,
    @ApplicationContext private val context: Context,
) : ViewModel() {
    data class UiState(
        val instruction: NavigationInstruction? = null,
        val isLoading: Boolean = false,
        val errorMessage: String = "",
    )
    // UiState + events: Channel<NavigationEvent> — 기존 패턴과 동일

    init {
        viewModelScope.launch {
            stateHolder.currentInstruction.collect { instruction ->
                _uiState.update { it.copy(instruction = instruction, isLoading = false) }
                if (instruction?.action == NavigationAction.ARRIVED) {
                    _events.send(NavigationEvent.NavigateToEnd)
                }
            }
        }
    }

    @SuppressLint("MissingPermission")
    fun startNavigation(sessionId: String, destLat: Double, destLng: Double, destRadius: Double) {
        viewModelScope.launch {
            _uiState.update { it.copy(isLoading = true) }
            // 마지막 알려진 위치로 경로 요청
            val fusedClient = LocationServices.getFusedLocationProviderClient(context)
            val startLocation = suspendCancellableCoroutine { cont ->
                fusedClient.lastLocation
                    .addOnSuccessListener { loc -> cont.resume(loc) }
                    .addOnFailureListener { cont.resume(null) }
            }
            val startLat = startLocation?.latitude ?: destLat
            val startLng = startLocation?.longitude ?: destLng

            fetchRouteUseCase(startLat, startLng, destLat, destLng)
                .onSuccess { steps ->
                    config.sessionId = sessionId
                    config.steps = steps
                    config.destinationLat = destLat
                    config.destinationLng = destLng
                    config.destinationRadius = destRadius
                    stateHolder.reset()
                    context.startForegroundService(Intent(context, NavigationService::class.java))
                    _uiState.update { it.copy(isLoading = false) }
                }
                .onFailure { error ->
                    _uiState.update { it.copy(isLoading = false, errorMessage = error.message ?: "경로를 불러오지 못했습니다.") }
                }
        }
    }

    fun cancelNavigation(sessionId: String) {
        viewModelScope.launch {
            runCatching { updateStatusUseCase(sessionId, "canceled") }
            context.stopService(Intent(context, NavigationService::class.java))
            stateHolder.reset()
            _events.send(NavigationEvent.NavigateToEnd)
        }
    }
}
```

### DestinationListViewModel 수정

```kotlin
// 이벤트에 Destination 추가
sealed class DestinationListEvent {
    data class NavigateToNavigation(val sessionId: String, val destination: Destination) : DestinationListEvent()
}

// startNavigation: destinations 목록에서 Destination 조회 후 이벤트에 포함
fun startNavigation(destinationId: String) {
    val destination = _uiState.value.destinations.find { it.destinationId == destinationId } ?: return
    viewModelScope.launch {
        createSessionUseCase(destinationId)
            .onSuccess { session ->
                _events.send(DestinationListEvent.NavigateToNavigation(session.sessionId, destination))
            }
            .onFailure { ... }
    }
}
```

---

## Screen / NavGraph 수정

### Screen.kt Navigation 라우트

```kotlin
data object Navigation : Screen(
    "navigation?sessionId={sessionId}&destLat={destLat}&destLng={destLng}&destRadius={destRadius}&destName={destName}"
) {
    fun createRoute(sessionId: String, destLat: Double, destLng: Double, destRadius: Double, destName: String) =
        "navigation?sessionId=$sessionId" +
        "&destLat=${destLat.toFloat()}" +
        "&destLng=${destLng.toFloat()}" +
        "&destRadius=${destRadius.toFloat()}" +
        "&destName=${android.net.Uri.encode(destName)}"
}
```

### NavGraph 수정 (DestinationList + Navigation)

```kotlin
// DestinationList: NavigateToNavigation 이벤트에 destination 정보 포함
is DestinationListEvent.NavigateToNavigation ->
    navController.navigate(
        Screen.Navigation.createRoute(
            event.sessionId,
            event.destination.latitude,
            event.destination.longitude,
            event.destination.radius,
            event.destination.name,
        )
    )

// Navigation composable
composable(
    route = Screen.Navigation.route,
    arguments = listOf(
        navArgument("sessionId")   { type = NavType.StringType; defaultValue = "" },
        navArgument("destLat")     { type = NavType.FloatType; defaultValue = 0f },
        navArgument("destLng")     { type = NavType.FloatType; defaultValue = 0f },
        navArgument("destRadius")  { type = NavType.FloatType; defaultValue = 30f },
        navArgument("destName")    { type = NavType.StringType; defaultValue = "목적지" },
    ),
) { backStackEntry ->
    val viewModel: NavigationViewModel = hiltViewModel()
    LaunchedEffect(viewModel) {
        viewModel.events.collect { event ->
            when (event) {
                NavigationEvent.NavigateToEnd ->
                    navController.navigate(Screen.NavigationEnd.route) {
                        popUpTo(Screen.Navigation.route) { inclusive = true }
                    }
            }
        }
    }
    NavigationScreen(
        sessionId   = backStackEntry.arguments?.getString("sessionId").orEmpty(),
        destLat     = backStackEntry.arguments?.getFloat("destLat")?.toDouble() ?: 0.0,
        destLng     = backStackEntry.arguments?.getFloat("destLng")?.toDouble() ?: 0.0,
        destRadius  = backStackEntry.arguments?.getFloat("destRadius")?.toDouble() ?: 30.0,
        destName    = backStackEntry.arguments?.getString("destName") ?: "목적지",
        viewModel   = viewModel,
    )
}
```

### NavigationScreen 시그니처

```kotlin
@Composable
fun NavigationScreen(
    sessionId: String,
    destLat: Double,
    destLng: Double,
    destRadius: Double,
    destName: String = "목적지",
    viewModel: NavigationViewModel = hiltViewModel(),
)
```

- 위치 권한 확인: `rememberLauncherForActivityResult(RequestPermission)`
- 권한 있으면: `LaunchedEffect(Unit) { viewModel.startNavigation(sessionId, destLat, destLng, destRadius) }`
- `uiState.instruction`으로 actionLabel, remainingDistance 표시
- "안내 종료" 클릭 → `viewModel.cancelNavigation(sessionId)`
- 기존 `onEnd` 파라미터 제거 → ViewModel 이벤트로 처리

---

## AndroidManifest.xml 추가

```xml
<service
    android:name=".service.NavigationService"
    android:foregroundServiceType="location" />
```

---

## action → 표시 텍스트 매핑

| NavigationAction | actionLabel | 배경색 |
|-----------------|-------------|--------|
| STRAIGHT | 직진 | Ink |
| PREPARE_LEFT | 좌회전 준비 | Ink |
| LEFT | 좌회전 | Ink |
| PREPARE_RIGHT | 우회전 준비 | Ink |
| RIGHT | 우회전 | Ink |
| REROUTE | 경로 재탐색 | InkElevated |
| ARRIVED | 도착 | Ink |

---

## 에러 처리

| 상황 | 처리 |
|------|------|
| 경로 조회 실패 | errorMessage 표시, 서비스 미시작 |
| GPS 권한 없음 | 권한 요청 UI 표시 |
| action 전송 실패 | 로그 후 다음 주기 재시도 (세션 유지) |
| 경로 이탈 중 재탐색 실패 | 다음 위치 업데이트 시 재시도 |
| lastLocation null | destLat/destLng를 출발점으로 대체 |

## 미포함 항목

- NavigationEndScreen 데이터 (이동 시간/거리) — Phase 7에서는 mock 유지
- 마이페이지 DELETE 기능 — Phase 8
